<div class="text-center" id="blog" data-id="{{id}}">
    {{#if owner}}
    <div class="display-flex justify-space-between">
        <div class="col-md-4">
            <a href="/blogs/update/{{id}}"><button class="btn btn-sm btn-primary">Update Post</button></a>
        </div>
        <div class="col-md-4">
            <button class="btn btn-sm btn-danger" id="delete-btn">Delete Post</button>
        </div>
    </div>
    {{/if}}
    <h2>{{title}}</h2>
    <p>{{content}}</p>
    <p>Created by {{User.name}} on {{format_date createdAt}}</p>
    <form class="form" id="new-comment">
        <div class="form-group">
            <label for="content">Comment:</label>
            <input class="form-input" type="text" id="content" name="content"/>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary">Create</button>
        </div>
    </form>

    {{#if comments.length}}
    <div class="col">
        {{#each comments as |comment|}}
        <div class="row">
            <p>{{comment.content}} <br>
            posted by {{comment.User.name}} on {{format_date comment.createdAt}}</p>
        </div>
        {{/each}}
    </div>
    {{/if}}

    <script src="/js/comment.js"></script>
</div>